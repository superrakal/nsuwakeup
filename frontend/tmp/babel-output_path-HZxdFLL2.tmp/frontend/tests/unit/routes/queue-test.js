import { moduleFor, test } from 'ember-qunit';
moduleFor('route:queue', 'Unit | Route | queue', {});

test('it exists', function (assert) {
  var route;
  route = this.subject();
  return assert.ok(route);
});